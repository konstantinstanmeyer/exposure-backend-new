export default function Loading(){
    return (
        <div className="w-10 h-10 bg-white animate-spin mt-[25%] rounded-full flex items-center justify-center mx-auto">
            <p>{`...`}</p>
        </div>
    );
}